Context: All of Phase 2's commands (init, add, dev, inspect backend+frontend, doctor)
are built and individually tested. Build the final end-to-end suite that proves they
work together, plus the root-level verify script if it doesn't already exist from
Phase 1's QA work (check root package.json's scripts first).

If root package.json lacks a "verify" script, add:
  "verify": "pnpm lint && pnpm typecheck && pnpm build && pnpm test"

Create packages/cli/src/__e2e__/full-flow.test.ts (or an equivalent top-level e2e test
location - place it somewhere vitest.workspace.ts's existing include patterns will
pick up; if it doesn't, fix the include pattern now and note the fix in DECISIONS.md)
using vitest + node:fs.mkdtempSync(os.tmpdir()) to get a working directory OUTSIDE
the repo entirely:

1. Spawn the built CLI's `init e2e-test` inside the temp dir, assert success and file
   tree.
2. cd into it, spawn `add ping_tool`, assert the new tool file exists and is valid.
3. Run `pnpm install` and `pnpm build` inside that temp project (via execSafe, real
   process, this genuinely needs network/registry access for install - if this is
   too slow/flaky for CI later, that's a known tradeoff to document, not solve now).
4. Spawn `doctor`, assert exit code 0 on this clean project.
5. Inject a fake secret into one of the project's source files, spawn `doctor` again,
   assert non-zero exit AND assert (capturing full stdout) the fake secret's literal
   value does not appear anywhere in the output.
6. Spawn `inspect` against the project (with CLQ_INSPECT_PORT set explicitly to avoid
   colliding with anything else), re-run the core security assertions from Stage 4
   (forged Origin ? 403; missing token ? 401; valid request ? 200) against this live
   end-to-end instance, then shut it down cleanly via SIGINT and confirm its process
   and any child it spawned are both gone afterward (check via the captured PIDs).
7. Spawn `dev`, wait briefly, send SIGINT, confirm clean exit with no orphan (same
   pattern as Stage 3's own test, repeated here at the integration level on purpose).
8. At the very end of the test file, in an afterAll, do a final sweep asserting every
   PID captured anywhere in this test file is no longer present in the process table.

Write a MANUAL-BETA-CHECKLIST.md at the repo root (not inside any package) listing:
"Before any public announcement: recruit 5 developers outside the team, give them
zero context beyond the README, watch them run clq init through clq inspect without
helping, write down every point of friction or confusion as a bug - not a documentation
gap to patch around." Keep it to one paragraph plus a 5-item checklist of the exact
flow they should attempt unaided.

Append a final section "## Phase 2 - Complete" to ARCHITECTURE.md: confirm the exit
condition (this e2e test) passed, list the final CLI command surface (init, add, dev,
inspect, doctor), and confirm Phase 1's frozen core contracts were not modified by
anything built in this phase except the one explicitly-allowed additive change (if
any optional field was added to a core type - note it explicitly here if so, or state
"no core types were touched" if true, which is the expected and preferred outcome).

Run pnpm verify and the full e2e test yourself. This is the gate for the entire
phase - do not finish until every security assertion re-run here passes, not just
the happy-path flow.
