{
  "name": "{{projectName}}",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup",
    "start": "node dist/index.js"
  },
  "_comment_dependencies": "@clq-sh/core is pinned to workspace:* for Phase 2 local dev/testing. Once @clq-sh/core is published this becomes a real semver range (e.g. ^1.0.0).",
  "dependencies": {
    "@clq-sh/core": "^0.1.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.5.0"
  }
}
